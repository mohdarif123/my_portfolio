import { Box } from '@mui/material';
import { Typography } from '@mui/material';
import { aboutStyle } from './Above.styles';

const About =()=> {
    const classes = aboutStyle;
  return (
    <Box>
      <Typography>About</Typography>
    </Box>
  )
}

export default About;
